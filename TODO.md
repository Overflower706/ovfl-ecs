# TODO

## 현재 버전
**v3.4.1**

## 계획
- **`Phase`가 여섯인 것이 맞는지 아직 증거가 없다.** `Simulation`·`Reaction`·`View`의 경계는
  써 보기 전에는 확신하기 어렵다. 「Phase로 나눈다」가 「등록 순서대로 돈다」보다 나은지도 같이 본다.
- **`GetEntitiesWith`가 매 호출 `new List<Entity>()`를 만든다.** 목록을 호출자에게 돌려주므로
  `EntityListPool`을 쓰려면 반납 지점이 필요하고, 그것이 곧 호출부 변경이다.
  쓰레기보다 큰 비용은 **매 호출 전수 순회**이므로, 손을 댄다면 컴포넌트→엔티티 색인이 먼저다.
  프로파일러가 짚기 전에는 하지 않는다.
- **`IFixedCleanupSystem`은 쓰는 곳이 없다.** 다음 메이저에서 뺄 후보.

## 테스트 보완
